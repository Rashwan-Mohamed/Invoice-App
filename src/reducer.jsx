
export default function reducer({ state, action }) {
  switch (action.type) {
    case "add": {
      return state;
    }
    case "remove": {
      return state;
    }
    case "edit": {
      return state;
    }
  }
}
